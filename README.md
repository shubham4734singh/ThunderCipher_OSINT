# Thunder Infrastructure Setup

This repository contains the deployment scripts and infrastructure as code (IaC) templates for the Thunder backend systems.

## Prerequisites
- Python 3.9+
- Docker & Docker Compose
- Free-tier VPS (e.g., Oracle Always Free, GCP e2-micro, or a local VM)

## Usage
Run `python deployment.py --env prod` to initialize the stack using Docker Compose.
